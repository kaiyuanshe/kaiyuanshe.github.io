<%
  const category = path.split('/')[1];

  const posts = site.posts.filter(post =>

    post.categories.data.some(item  =>  item.name === category)
  );

  const data = {...site, posts};
%>

<%- include('./page', {site: data}) %>
