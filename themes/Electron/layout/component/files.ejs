<% files.forEach(file => { %>
  <li>
    <a target="_blank" href="<%= joinPath(path, file) %>">

      <i class="fas fa-2x fa-file-<%= fileType( file ) || 'alt' %>"></i>

      <span><%= file.split('/').slice(-1)[0] %></span>
    </a>
  </li>
<% }) %>
